const User = require("../../models/user/User");

// Register a new user

const userRegisterController = async (req, res) => {
  // business logic
  resizeBy.json({ user: "User Registered" });
};
module.exports = { user: userRegisterController };
